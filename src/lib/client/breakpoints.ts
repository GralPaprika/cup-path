/**
 * Tailwind default breakpoints (px). Keep in sync with Tailwind `md` / `lg`
 * used in CSS (`md:hidden`, `md:block`, etc.).
 */
export const BREAKPOINT_MD_PX = 768;
export const BREAKPOINT_LG_PX = 1024;

export const MIN_WIDTH_MD_QUERY = `(min-width: ${BREAKPOINT_MD_PX}px)`;
export const MIN_WIDTH_LG_QUERY = `(min-width: ${BREAKPOINT_LG_PX}px)`;
